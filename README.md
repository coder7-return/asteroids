Boot.Dev

Build Asteroids using Python and Pygame (source code for all chapters)

Game Feature Enhancements (haven't yet implemented)

    Add a scoring system.

    Implement multiple lives and respawning.

    Add an explosion effect for the asteroids.

    Add acceleration to the player movement (thrust).

    Make the objects wrap around the screen instead of disappearing (toroidal map).

    Add a background image.

    Create different weapon types.

    Make the asteroids lumpy instead of perfectly round (non-uniform collision shapes).

    Make the ship have a triangular hit box instead of a circular one.

    Add a shield power-up.

    Add a speed power-up.

    Add bombs that can be dropped (area-of-effect weapon).
